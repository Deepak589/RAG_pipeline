# RAG_dev — ragenv
# Ollama accessed via stdlib urllib (no client lib needed).

numpy==2.3.5
pypdfium2==5.9.0

# Dense embeddings (pulls torch, transformers, scikit-learn)
sentence-transformers==5.6.0
